<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Своя тема на базе платформенной. Библиотеки тем (AppCompat, Material, Leanback)
         намеренно не подключаются: каждая тянет мегабайты ресурсов ради стилей,
         которые на телевизоре всё равно переопределяются. См. ADR-002 и бюджет веса. -->
    <style name="Theme.Istok" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
