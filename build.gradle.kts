// Версия задаётся здесь напрямую, без version catalog: зависимостей в проекте
// намеренно почти нет (ADR-009), и отдельный файл каталога только добавил бы косвенность.
//
// Плагин Kotlin отдельно не подключается: начиная с AGP 9.0 поддержка Kotlin встроена
// в сам плагин Android, а внешний org.jetbrains.kotlin.android с ним конфликтует.
// Побочная выгода - одной версией под контролем меньше.
plugins {
    id("com.android.application") version "9.3.2" apply false
}
